#include <stdio.h>
#include <stdlib.h>


typedef struct node{
	int val;
	struct node * left;
	struct node * right;
	struct node * parent;
} node;

int main(){
	node* head = NULL;
	head = malloc(sizeof(node));
	if(head == NULL){
		return 1;
	}
	head->val = 1;
	head->right = NULL;
	head->left = NULL;
	return 0;
}
